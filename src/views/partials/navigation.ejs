<nav class="nav">
    <h2 class="nav_logo">file app</h2>
    <div class="nav_menu">
    <% if(locals.user) { %>
        <h2>Welcome <%= locals.user.username %></h2>
        <a class="nav_link" href="/user/settings">settings</a>
        <form method="GET" action="/auth/logout">
            <button class="nav_link">log out</button>
        </form>
    <% } else { %>
        <a class="nav_link" href="/auth/login">login</a>
        <a class="nav_link" href="/auth/register">register</a>
    <% } %>

      
    </div>
</nav>